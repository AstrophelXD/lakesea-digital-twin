# 前端（Vue 3 + TypeScript）

## 快速启动

```bash
cd frontend
npm install
npm run dev
```

浏览器访问 http://localhost:5173 ，开发服务器已将 `/api` 代理到后端 `http://127.0.0.1:8000`。

## 构建

```bash
npm run build
npm run preview
```
